<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{{ title }} - Wiki Bas Gros Poing</title>
    
    <link rel="stylesheet" href="assets/pico.min.css">
    <style>

      iframe {
        max-width: 100%;
        aspect-ratio: 16/9;
      }

      article img {
        float:initial;
        display:inline;
        margin:unset;
      }

      .toc * {
        all: revert;
      }

      thead {
        background:var(--form-element-background-color);
      }
    </style>

    <meta name="description" content="Page de wiki au sujet de {{ title }}">
    <meta property="og:title" content="{{ title }} - Wiki Bas Gros Poing">
    <meta property="og:description" content="Page de wiki au sujet de {{ title }}">
    <meta property="og:image" content="https://www.mywebsite.com/image.jpg">
    <meta property="og:image:alt" content="Image description">
    <meta property="og:locale" content="fr_FR">
    <meta property="og:type" content="website">
    <meta name="twitter:card" content="summary_large_image">
    <meta property="og:url" content="https://www.mywebsite.com/page">
    <link rel="canonical" href="https://wiki.basgrospoing.fr/{{slug}}">

    <link rel="icon" href="/favicon.ico">
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <link rel="manifest" href="/my.webmanifest">
    <meta name="theme-color" content="#FF00FF">

  </head>

  <body>
    <main class="container">
      <nav>
        <ul>
          <li>
            <strong>Wiki Bas Gros Poing</strong>
          </li>
        </ul>
        <ul>
          <li>
            <a href="/">Accueil</a>
          </li>
          <li>
            <a href="https://github.com/Thomasorus/wiki-basgrospoing">Code source</a>
          </li>

        </ul>
      </nav>
      <article>
        <h1>{{ title }}
        </h1>
        {{ content | safe }}
        <h2>Les jeux</h2>
        <ul>
          {%- for game in collections.game -%}
            <li>
              <a href="{{ game.data.permalink }}">{{ game.data.title }}</a>
            </li>
          {%- endfor -%}
        </ul>
        <h2>Guides divers</h2>
        <ul>
          {%- for other in collections.other -%}
            <li>
              <a href="{{ other.data.permalink }}">{{ other.data.title }}</a>
            </li>
          {%- endfor -%}
        </ul>
        {# <pre>
          [
          {%- for character in collections.character -%}
            {
              "slug": "{{ character.data.slug }}",
              "old": "{{ character.data.redirect }}",
              "slugify": "{{character.data.slug | slugify }}"
            },
          {%- endfor -%}
           {%- for game in collections.game -%}
            {
              "slug": "{{ game.data.slug }}",
              "old": "{{ game.data.redirect }}",
              "slugify": "{{game.data.slug | slugify }}"
            },
          {%- endfor -%}
          {%- for other in collections.other -%}
            {
              "slug": "{{ other.data.slug }}",
              "old": "{{ other.data.redirect }}",
              "slugify": "{{other.data.slug | slugify }}"
            },
          {%- endfor -%}
          ]
          </pre> #}
         <footer>
        Initially last edited: {{ date }} by {{ author }}.
      </footer>
      </article>
    </main>
   
  </body>
</html>